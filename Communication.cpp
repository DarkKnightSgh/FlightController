#include "Communication.h"

void Communication::initialize() {
    // Initialize communication (e.g., Serial, I2C, SPI)
}

void Communication::sendData(float roll, float pitch, float yaw, float altitude) {
    // Send data to companion computer
}
